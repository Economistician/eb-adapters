"""
Test package for eb-adapters integration and adapter classes.

This module is intentionally empty; it just marks the `tests.adapters`
directory as a package so pytest and import machinery can resolve
relative imports cleanly.
"""
